import Vue from 'vue'
import Vuex from 'vuex'

import controller from './controller'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
     controller,
  }
})
